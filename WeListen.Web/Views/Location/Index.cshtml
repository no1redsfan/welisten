@model WeListen.Web.Viewmodels.Locations.Index

@{
    ViewBag.Title = "View1";
}

<h2>View1</h2>

<p>
    
</p>
<table>
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model.Locations) {
    <tr>
        <td>
            @item.Name
            @*@Html.ActionLink("Enter Location", "Index", new { id=item.LocationId  }) *@
            <input type="submit" value="Enter Location" onclick="location.href='@Url.Action("Index", "Location", new {location=@item})'" />
            
        </td>
    </tr>
}

</table>
